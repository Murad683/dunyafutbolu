# ── Server ──
PORT=3001

# ── Database (Option A: Connection URL — preferred for cloud) ──
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# DB_SSL=true

# ── Database (Option B: Individual vars — for local dev) ──
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=dunyafutbolu

# ── Auth ──
JWT_SECRET=your-jwt-secret-here
